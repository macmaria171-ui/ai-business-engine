{
  "id": "code-bundle-1",
  "type": "code-bundle",
  "niche": "automatización",
  "title_es": "Scripts de Automatización con Python",
  "title_en": "Python Automation Scripts Bundle",
  "tagline": "Ahorra horas con código listo para usar",
  "description_es": "Colección de 10 scripts en Python para tareas comunes: scraping, envío de emails, generación de reportes y manejo de APIs, con documentación paso a paso.",
  "price_usd": 18,
  "tags": [
    "python",
    "automation",
    "scripts",
    "productivity"
  ],
  "target_audience": "Desarrolladores y profesionales que buscan automatizar procesos",
  "what_they_get": "Herramientas listas para integrar y reducir trabajo manual",
  "status": "ready",
  "generated_at": "2026-06-11T02:05:30.163571",
  "content_preview": "See full product for content",
  "full_content": "**Automatización con Python**\n\nA continuación, se presentan dos ejemplos de scripts de automatización en Python. Estos scripts se pueden utilizar para realizar tareas de manera automatizada y eficiente.\n\n### Script 1: Automatización de la actualización de paquetes en Linux\n\nEste script se encarga de actualizar los paquetes en un sistema Linux de manera automatizada.\n\n#### scripts/update_paquetes.py\n```python\n#!/usr/bin/env python3\n\n\"\"\"\nScript para actualizar los paquetes en un sistema Linux de manera automatizada.\n\nRequiere:\n- Acceso root al sistema\n- Paquete apt o dnf instalado\n\"\"\"\n\nimport subprocess\nimport os\n\ndef actualizar_paquetes():\n    \"\"\"\n    Actualiza los paquetes en el sistema Linux.\n    \"\"\"\n    # Verificar si se tiene acceso root\n    if os.geteuid() != 0:\n        print(\"Error: Necesita acceso root para ejecutar este script.\")\n        return\n\n    # Seleccionar el paquete de gestión de paquetes\n    package_manager = \"apt\"  # apt o dnf\n    if package_manager == \"apt\":\n        comando = \"sudo apt update && sudo apt full-upgrade -y\"\n    elif package_manager == \"dnf\":\n        comando = \"sudo dnf check-update && sudo dnf upgrade -y\"\n\n    # Ejecutar el comando\n    try:\n        subprocess.run(comando, shell=True)\n        print(\"Paquetes actualizados con éxito.\")\n    except Exception as e:\n        print(f\"Error al actualizar paquetes: {e}\")\n\nif __name__ == \"__main__\":\n    actualizar_paquetes()\n```\n\n#### README.md\n```markdown\n# Actualizar paquetes en Linux de manera automatizada\n\nEste script se encarga de actualizar los paquetes en un sistema Linux de manera automatizada.\n\n## Requisitos\n\n* Acceso root al sistema\n* Paquete `apt` o `dnf` instalado\n\n## Uso\n\n1. Edite el script `update_paquetes.py` y ajuste el paquete de gestión de paquetes (`apt` o `dnf`) según sea necesario.\n2. Ejecute el script con permisos de superusuario (root): `sudo python3 update_paquetes.py`\n\n## Ejemplo de uso\n```bash\nsudo python3 update_paquetes.py\n```\n```\n\n### Script 2: Automatización de respaldos de bases de datos MySQL\n\nEste script se encarga de realizar respaldos de bases de datos MySQL de manera automatizada.\n\n#### scripts/respaldar_bases.py\n```python\n#!/usr/bin/env python3\n\n\"\"\"\nScript para realizar respaldos de bases de datos MySQL de manera automatizada.\n\nRequiere:\n- Acceso root a la base de datos\n- Paquete `mysql-connector-python` instalado\n\"\"\"\n\nimport mysql.connector\nimport os\nimport datetime\n\ndef respaldar_base():\n    \"\"\"\n    Realiza un respaldo de la base de datos MySQL.\n    \"\"\"\n    # Verificar si se tiene acceso root a la base de datos\n    if not os.environ.get(\"DB_HOST\") or not os.environ.get(\"DB_USER\") or not os.environ.get(\"DB_PASSWORD\"):\n        print(\"Error: Necesita proporcionar la información de conexión a la base de datos.\")\n        return\n\n    # Seleccionar la base de datos a respaldar\n    database = \"mi_base_de_datos\"\n\n    # Conectar a la base de datos\n    try:\n        cnx = mysql.connector.connect(\n            host=os.environ.get(\"DB_HOST\"),\n            user=os.environ.get(\"DB_USER\"),\n            password=os.environ.get(\"DB_PASSWORD\"),\n            database=database\n        )\n    except mysql.connector.Error as err:\n        print(f\"Error al conectar a la base de datos: {err}\")\n        return\n\n    # Realizar el respaldo\n    try:\n        cursor = cnx.cursor()\n        comando = f\"mysqldump -u {os.environ.get('DB_USER')} -p{os.environ.get('DB_PASSWORD')} {database} > respaldo_{datetime.date.today().strftime('%Y%m%d')}.sql\"\n        subprocess.run(comando, shell=True)\n        print(\"Base de datos respaldada con éxito.\")\n    except Exception as e:\n        print(f\"Error al realizar el respaldo: {e}\")\n\n    # Cerrar la conexión\n    cnx.close()\n\nif __name__ == \"__main__\":\n    respaldar_base()\n```\n\n#### README.md\n```markdown\n# Realizar respaldos de bases de datos MySQL de manera automatizada\n\nEste script se encarga de realizar respaldos de bases de datos MySQL de manera automatizada.\n\n## Requisitos\n\n* Acceso root a la base de datos\n* Paquete `mysql-connector-python` instalado\n\n## Uso\n\n1. Edite el script `respaldar_bases.py` y ajuste la información de conexión a la base de datos según sea necesario.\n2. Ejecute el script con permisos de superusuario (root): `sudo python3 respaldar_bases.py`\n\n## Ejemplo de uso\n```bash\nsudo DB_HOST=localhost DB_USER=root DB_PASSWORD=password python3 respaldar_bases.py\n```\n```\n\nRecuerde ajustar la información de conexión a la base de datos en el script `respaldar_bases.py` según sea necesario."
}